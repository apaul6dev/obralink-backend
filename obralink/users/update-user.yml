info:
  name: Update User
  type: http
  seq: 4
  tags:
    - users
http:
  method: PATCH
  url: "{{baseUrl}}/identity/users/{{userId}}"
  params:
    - name: userId
      value: "{{userId}}"
      type: path
  headers:
    - name: Content-Type
      value: application/json
    - name: x-tracking-id
      value: "{{trackingId}}"
  body:
    type: json
    data: |-
      {
        "firstName": "Updated",
        "lastName": "User",
        "identificationNumber": "0912345678",
        "personalEmail": "updated.personal@example.com",
        "phoneNumber": "+1 555 765 4321",
        "status": "ACTIVE"
      }
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
docs: |-
  Requires `COMPANY_ADMIN` and `identity.users.update`.
